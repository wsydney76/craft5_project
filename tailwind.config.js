// const colors = require('tailwindcss/colors')

module.exports = {

    // mode: process.env.NODE_ENV ? 'jit' : undefined,

    theme: {
        extend: {},
    },

    variants: {
        extend: {},
    },

    plugins: [],

    purge: [
        'templates/**/*.twig'
    ]
}
