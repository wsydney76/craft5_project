{% from '_macros/lib.twig' import img %}

{% if featuredImage %}
    <figure class="pt-8">
        {{ img(featuredImage, {
            transform: {width: 960, height: 300},
            srcset: [960, 640, 400],
            attr: {loading: null}
        }) }}
    </figure>

{% endif %}
