{% set target = entry.target.one %}
{% if target %}
    {% include '_layouts/partials/button.twig' with {
        type: 'link',
        url: target.url,
        caption: entry.caption ?: target.title
    } only %}
{% endif %}